{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "tsc --noEmit",
    "typegen": "nanoflare types",
    "deploy": "npm run build && nanoflare deploy"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.5.2"
  },
  "devDependencies": {
    "@nanoflare/cli": "^0.4.0",
    "@nanoflare/vite-plugin": "^0.1.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  }
}
