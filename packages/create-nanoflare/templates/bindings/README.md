# Worker bindings

Create a KV namespace and object-storage bucket, then replace both placeholder IDs in `nanoflare.json`. `GET` and `PUT /kv/:key` use KV; `GET` and `PUT /objects/:key` use object storage.
