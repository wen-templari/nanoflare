{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "check": "tsc --noEmit",
    "typegen": "nanoflare types",
    "deploy": "npm run build && nanoflare deploy"
  },
  "devDependencies": {
    "@nanoflare/cli": "^0.2.3",
    "@nanoflare/vite-plugin": "^0.0.4",
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  }
}
