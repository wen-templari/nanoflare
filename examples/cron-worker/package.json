{
  "name": "@nanoflare/cron-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "deploy": "$npm_execpath run build && nanoflare deploy",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@nanoflare/cli": "^0.1.0",
    "@nanoflare/workers-types": "file:../../packages/workers-types",
    "typescript": "^5.8.3"
  }
}
