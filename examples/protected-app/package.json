{
  "name": "@nanoflare/protected-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@nanoflare/workers-types": "file:../../packages/workers-types",
    "typescript": "^5.8.3"
  }
}
